# 本番ビルド(pnpm build)で使われるAPIの向き先。
# TODO: 引き継いだ人は、自分のWorkerのURLに差し替えること。
# wrangler.jsonc の vars.WEB_ORIGIN / BETTER_AUTH_URL と同じ値にする
# (同じWorkerがフロントも配信するため、3つとも同一オリジンになる)。
VITE_API_URL=https://TODO-your-worker.workers.dev
