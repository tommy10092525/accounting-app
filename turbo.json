{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^build", "^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "deploy": {
      // wrangler.jsonc の assets が ../web/dist を配信するため、
      // Workerをデプロイする前に必ずフロントのビルドを終わらせる必要がある。
      "dependsOn": ["web#build"],
      "cache": false,
      "persistent":true,
      "interactive":true
    }
  }
}
